@model Faculty
@{
    ViewData["Title"] = "Delete Faculty";
    ViewData["HeaderClass"] = "sub-header";
    ViewData["HeaderContent"] = "<h1>Delete Faculty</h1>";
}

<section class="about-us">
    <div class="container">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header bg-danger text-white">
                        <h3 class="mb-0"><i class="fas fa-exclamation-triangle me-2"></i>Delete Faculty</h3>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-danger">
                            <h4>Are you sure you want to delete this faculty?</h4>
                            <p>This action cannot be undone and may affect departments, courses, and users associated with this faculty.</p>
                        </div>

                        <div class="faculty-details mb-4">
                            <h5>Faculty Details</h5>
                            <dl class="row">
                                <dt class="col-sm-3">Name</dt>
                                <dd class="col-sm-9">@Model.Name</dd>

                                <dt class="col-sm-3">Description</dt>
                                <dd class="col-sm-9">@Model.Description</dd>
                            </dl>
                        </div>

                        <form asp-action="DeleteFaculty" method="post">
                            <input type="hidden" asp-for="Id" />
                            <div class="form-group d-flex justify-content-between">
                                <a asp-action="ManageFaculties" class="btn btn-secondary">Cancel</a>
                                <button type="submit" class="btn btn-danger">Delete Faculty</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>